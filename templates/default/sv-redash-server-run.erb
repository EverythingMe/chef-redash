#!/bin/sh
cd <%= ::File.join(node['redash']['path'], 'current') %>
exec 2>&1
. <%= ::File.join(node['redash']['path'], 'shared', 'env.sh') %>
exec chpst -u <%= node['redash']['user'] %> env <%= ::File.join(node['redash']['path'], 'virtualenv', 'bin', 'gunicorn') %> -b 127.0.0.1:5000 --name redash -w 4 redash:app

